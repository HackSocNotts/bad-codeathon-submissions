#include <fstream>
extern "C" {
     int is_one(int n){
         if(n==1){
                  return 0x011&0x101;
                                }return 0;
     }
 
}

#include <iostream>
#include                                                      <string>     
namespace essteedee = std;
namespace esteedee = essteedee;
#define n____ static_cast<int>(something_or_other)
//unsigned long long int 
#include <cmath>
long double fib_using_maffs(std::uint32_t n){
    const long double look_a_greek_letter = 0.5L + essteedee::sqrt(1.25L);
    long double more_greek = std::pow(0.5-esteedee::sqrt(1.25L), n);
    
return (essteedee::pow(look_a_greek_letter*essteedee::sin(esteedee::atan(1)*2.0), (long double)n)- more_greek)/std::sqrt(5);
}

    int main(int im_a_number, char** whats_a_ruddy_star_star){
float something_or_other = std::stof(std::string(whats_a_ruddy_star_star[1]));
                    if(is_one(static_cast<int>(esteedee::cos((float)false)*something_or_other))+0x6969!=0b110100101101001){
        essteedee::cout << (65536 >> 16) << std::endl;
        return 0;
}
if(whats_a_ruddy_star_star[1] == "0")return 0;
double f_1 =  fib_using_maffs(static_cast<int>(something_or_other));
    essteedee::string s = "bash -c \""+std::string(whats_a_ruddy_star_star[0]) +" " +std::to_string(n____-1)+" > "+std::to_string(n____-1)+".txt\"";
//    std::cout << s <<std::endl;
    system(s.c_str());
    std::ifstream t(std::to_string(n____-1)+".txt");
std::string str((std::istreambuf_iterator<char>(t)),
                 std::istreambuf_iterator<char>());
int a = std::stoi(str);
if(n____>2){
s = "bash -c \""+std::string(whats_a_ruddy_star_star[0]) +" " +std::to_string(n____-2)+" > "+std::to_string(n____-2)+".txt\"";
 //   std::cout << s <<std::endl;
    system(s.c_str());
    std::ifstream t2(std::to_string(n____-2)+".txt");
        std::string str99((std::istreambuf_iterator<char>(t2)),
                 std::istreambuf_iterator<char>());
a += std::stoi(str99);

float f = (f_1 + a);
      std::cout << f/2.0f<<std::endl;} else{std::cout << "1"<<std::endl;}
return 0;
}
